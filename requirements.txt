fastapi>=0.115
uvicorn[standard]>=0.30
aiortc>=1.9
av>=12.3
opencv-python-headless>=4.10
numpy>=1.26
ultralytics>=8.3
qrcode[pil]>=7.4
pydantic>=2.8
psutil>=6.0

# Required by persistent tracking; install before the first camera frame.
lap>=0.5.12
# Dataset configuration for the offline training helper (already used by Ultralytics).
PyYAML>=6.0
